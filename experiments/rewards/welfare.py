import igraph as ig
import pandas as pd
from typing import List


def welfare(g: ig.Graph, census_data: pd.DataFrame, groups: List[str] = None) -> float:
    """
    This reward gives most weighting to improve the
    Args:
        g:
        census_data:
        groups:

    Returns:

    """
    raise NotImplementedError()
